import App from "./app.js";

const main = () => {
  let app = new App();

  /* You can add cards to the board here so you don't have to type them all in every time the page refreshes. Here are a few examples: */
  // app.addCard("doing", "Write Card class", "lightblue");
  // app.addCard("todo", "Write App class", "khaki");
  // let card = app.addCard("todo", "Test everything!", "pink");
  // card.setDescription("Hopefully we've been testing throughout the process...");

  // app.addCard("todo", "green: white font", "#008000");
  // app.addCard("todo", "red: white font", "#FF0000");
  // app.addCard("todo", "purple: white font", "#800080");
  // app.addCard("todo", "black: white font", "#000000");
  // app.addCard("todo", "brown: white font", "#A52A2A");
  // app.addCard("todo", "yellow: black font", "#FFFF00");
  // app.addCard("todo", "orange: black font", "#FFA500");
  // app.addCard("todo", "pink: black font", "#FFC0CB");
  // app.addCard("todo", "white: black font", "#FFFFFF");
};
main();
